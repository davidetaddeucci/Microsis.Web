@using Syncfusion.Licensing

@{
    // Registra la licenza Syncfusion se disponibile
    var license = ViewData["SyncfusionLicense"] as string ?? "";
    if (!string.IsNullOrEmpty(license))
    {
        SyncfusionLicenseProvider.RegisterLicense(license);
    }
}
